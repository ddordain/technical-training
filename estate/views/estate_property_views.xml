<?xml version="1.0" encoding="UTF-8"?>
<odoo>

  <!-- Custom Search View -->
  <record id="estate_property_view_search" model="ir.ui.view">
    <field name="name">estate.property.search</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <search>
        <field name="name" string="Title"/>
        <field name="postcode"/>
        <field name="bedrooms"/>
        <field name="living_area" string="Living Area (sqm)"/>
        <field name="expected_price" class="underline-input"/>
        <field name="facades"/>
      </search>
    </field>
  </record>


  <!-- Custom Tree View -->
  <record id="estate_property_view_tree" model="ir.ui.view">
    <field name="name">estate.property.tree</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <tree decoration-muted="state=='sold'" decoration-success="state in ['offer_accepted', 'offer_received']" decoration-bf="state == 'offer_accepted'">
        <field name="name" string="Title"/>
        <field name="postcode"/>
        <field name="tags_id" widget="many2many_tags" options="{'color_field': 'color'}"/>
        <field name="bedrooms"/>
        <field name="living_area" string="Living Area (sqm)"/>
        <field name="expected_price" class="underline-input"/>
        <field name="selling_price"/>
        <field name="date_availability" string="Available From" optional="hide"/>
        <field name="state" invisible="True"/>
      </tree>
    </field>
  </record>

  <!-- Custom Form View -->
  <record id="estate_property_view_form" model="ir.ui.view">
    <field name="name">estate.property.form</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <form>
        <header>
          <button name="action_sold" type="object" string="SOLD" class="btn-success" states="offer_accepted"/>
          <button name="action_cancel" type="object" string="CANCEL" class="btn-danger" states="new, offer_received"/>
          <field name="state" widget="statusbar" statusbar_visible="new, offer_received,offer_accepted,sold"/>
        </header>
        <sheet>
          <h1>
            <field name="name"/>
          </h1>
          <field name="tags_id" widget="many2many_tags" options="{'color_field': 'color'}"/>
          <group>
            <group>
              <field name="property_type_id" options="{'no_create': True}"/>
              <field name="postcode"/>
              <field name="date_availability" string="Available From"/>
            </group>
            <group>
              <field name="expected_price" class="underline-input"/>
              <field name="best_offer"/>
              <field name="selling_price"/>
            </group>
          </group>
          <notebook>
            <page string="Description">
              <group>
                <field name="description"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="facades"/>
                <field name="garage"/>
                <field name="garden"/>
                <field name="garden_area" string="Garden Area (sqm)" attrs="{'invisible': [('garden', '=', False)]}"/>
                <field name="garden_orientation" attrs="{'invisible': [('garden', '=', False)]}"/>
                <field name="total_area" string="Total area (sqm)"/>
              </group>
            </page>
            <page string="Offers">
              <field name="offer_ids" nolabel="1" attrs="{'readonly': [('state', 'in', ['offer_accepted', 'sold', 'canceled'])]}">
                <tree editable="bottom" decoration-success="status=='accepted'" decoration-danger="status=='refused'">
                  <field name="price"/>
                  <field name="partner_id"/>
                  <field name="validity"/>
                  <field name="date_deadline"/> 
                  <button name="action_accept" type="object" icon="fa-check" title="Accept" attrs="{'invisible': [('status', 'in', ['accepted', 'refused'])]}"/>
                  <button name="action_refuse" type="object" icon="fa-times" title="Refuse" attrs="{'invisible': [('status', 'in', ['accepted', 'refused'])]}"/>
                  <field name="status" invisible="True"/>
                </tree>
              </field>
            </page>
            <page string="Other Info">
              <group>
                <field name="salesperson_id"/>
                <field name="buyer_id"/>
              </group>
            </page>
          </notebook>
        </sheet>
      </form>
    </field>
  </record>


<!-- Custom Kanban View -->
<record id="estate_property_view_kanban" model="ir.ui.view">
    <field name="name">estate.property.kanban</field>
    <field name="model">estate.property</field>
    <field name="arch" type="xml">
      <kanban default_group_by="property_type_id" records_draggable="False">
        <templates>
          <t t-name="kanban-box">
            <div class="oe_kanban_global_click">
              <h1 class="o_kanban_record_title">
                <field name="name"/> 
                <field name="state" invisible="True"/>
              </h1>
              <div>
                Expected Price: <field name="expected_price"/>
              </div>
              <t t-if="record.state.raw_value == 'offer_received'">
                <div>
                  Best Offer: <field name="best_offer"/>
                </div>
              </t>
              <t t-if="record.state.raw_value == 'sold' or record.state.raw_value == 'offer_accepted'">
                <div>
                  Selling Price: <field name="selling_price"/>
                </div>
              </t>
              <div>
                <field name="tags_id" widget="many2many_tags" options="{'color_field': 'color'}"/>
              </div>
            </div>
          </t>
        </templates>
      </kanban>
    </field>
</record>


  <record id="estate_property_action" model="ir.actions.act_window">
      <field name="name">Properties</field>
      <field name="res_model">estate.property</field>
      <field name="view_mode">tree,form,search,kanban</field>
      <field name="context">{'search_default_available': True}</field>
  </record>

</odoo>

